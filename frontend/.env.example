# URL base de la API del backend (Express).
# En desarrollo local apunta al backend que arrancas con `npm run dev` en /backend.
# En producción Vercel inyecta este valor desde el dashboard del proyecto.
VITE_API_URL=http://localhost:3000/api
